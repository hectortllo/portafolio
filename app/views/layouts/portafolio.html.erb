<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title><%= @page_title %></title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <meta name="keywords" content="<%= @seo_keywords %>" />

    <%= stylesheet_link_tag 'portafolios', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_pack_tag 'portafolios', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'portafolios', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <%= render 'shared/portafolio_nav'%>

    <%= render 'shared/portafolio_masthead'%>

    <%# Permite conectar con cualquier otro archivo html%>
    <%= yield %>
    <%= render 'shared/portafolio_footer'%>
    <%# Llamando el método source_helper que está en el archivo application_helper.rb%>
    <%= source_helper("application") %>
  </body>
</html>
