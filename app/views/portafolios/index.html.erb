<h1>Portafolio items</h1>

<%# link_to viene siendo la etiqueta a href de HTML %>
<%# Línea para mandar a la página new_portaflio_url al dar click
 en Create New Item %>
<%= link_to "Create New Item", new_portafolio_url %>

<%@portfolio_items.each do |portfolio_item| %>
    <p><%= portfolio_item.title %></p>
    <p><%= portfolio_item.subtitle %></p>
    <p><%= portfolio_item.body %></p>
    <%= image_tag portfolio_item.thumb_image unless portfolio_item.thumb_image.nil? %>
    <%= link_to "Edit", edit_portafolio_path(portfolio_item.id) %>
<%end%>